# P2-M1c Phase 0 Review

- **Reviewer**: Tester (Gate G0)
- **Date**: 2026-03-18
- **Commit**: `2a5a7b29814719cee8d9827f00c9825805bfa495`
- **Branch**: `feat/backend-m1c-impl`
- **Conclusion**: **PASS**

## Scope

Phase 0 delivers: GLOSSARY updates, WRAPPER_TOOL_EVAL_CONTRACT_V1, GC-3 deferral decision, bd feasibility checklist. 4 files changed, +235 lines.

## Findings

No P0 or P1 findings.

### P2 (low-risk observations)

| # | Severity | File | Line(s) | Description |
|---|----------|------|---------|-------------|
| F1 | P2 | `src/growth/contracts.py` | 167 | `WRAPPER_TOOL_EVAL_CONTRACT_V1.version=1` shares the same version number as the skeleton (`WRAPPER_TOOL_EVAL_CONTRACT_SKELETON.version=1`). Not a conflict because `contract_id` is distinct (`wrapper_tool_eval_v1` vs `wrapper_tool_skeleton_v1`), but future tooling that groups by `(object_kind, version)` without `contract_id` would see ambiguity. Acceptable in V1; worth noting for contract registry evolution. |
| F2 | P3 | `dev_docs/cases/gc3_import_deferred.md` | 27-43 | bd feasibility checklist `Verified` column is empty. This is by design (Phase 1 spike will populate it), but Phase 1 should explicitly mark each row pass/fail with a date. |

## Requirement-Evidence Matrix

### 1. ADR Consistency

| Check | Evidence | Verdict |
|-------|----------|---------|
| GLOSSARY `wrapper_tool` definition aligns with ADR 0056 (single-turn, governed, no cross-turn state) | GLOSSARY line 171: "single-turn governed capability unit ... 不承载 cross-turn state（ADR 0056）" | PASS |
| GLOSSARY `artifact_id` aligns with ADR 0053/0055 (UUIDv7) | GLOSSARY line 345: "采用 UUIDv7（ADR 0053 / 0055）" | PASS |
| GLOSSARY `BuilderTaskRecord` aligns with ADR 0055 (canonical = workspace artifact) | GLOSSARY line 319: "canonical record 在 `workspace/artifacts/` 下的 workspace artifact 中（ADR 0055）" with `persisted-in → workspace/artifacts/` and `indexed-by → bd / beads issues` | PASS |

### 2. Contract V1 Coverage

| Check | Evidence | Verdict |
|-------|----------|---------|
| `WRAPPER_TOOL_EVAL_CONTRACT_V1` has 5 `required_checks` | `contracts.py` lines 186-193: `typed_io_validation`, `permission_boundary`, `dry_run_smoke`, `before_after_cases`, `scope_claim_consistency` | PASS |
| `_CONTRACTS[GrowthObjectKind.wrapper_tool]` points to V1, not skeleton | `contracts.py` line 305: `GrowthObjectKind.wrapper_tool: WRAPPER_TOOL_EVAL_CONTRACT_V1` | PASS |
| Skeleton retained but not in runtime registry | `contracts.py` lines 117-163: skeleton still defined; line 305 maps to V1; test `TestWrapperToolSkeletonHistorical::test_skeleton_not_in_registry` confirms | PASS |
| `contract_id` is `"wrapper_tool_eval_v1"` | `contracts.py` line 168; test `TestWrapperToolContractV1::test_contract_id` asserts exact value | PASS |
| ADR 0054 immutable contract principle respected (skeleton not mutated, new V1 constant created) | Skeleton comment updated to "historical -- superseded by V1" at line 116, but the constant itself is unchanged (same `contract_id="wrapper_tool_skeleton_v1"`, same fields). New V1 is a separate frozen constant. Skeleton remains frozen (`test_skeleton_frozen`). | PASS |

### 3. GC-3 Decision

| Check | Evidence | Verdict |
|-------|----------|---------|
| GC-3 deferred status documented | `dev_docs/cases/gc3_import_deferred.md` line 9: "推迟，不纳入 P2-M1c acceptance criteria" | PASS |
| Reason recorded | Lines 13-16: import protocol not frozen; P2-M1c core loop is GC-1/GC-2 not GC-3; ADR 0057 guidance; accumulate evidence first | PASS |
| Recovery condition stated | Lines 20-21: "推迟到 P2-M2 或后续 milestone"; line 22: "若 GC-1/GC-2 执行过程中发现 import 是硬依赖，可重新评估" | PASS |

### 4. bd Feasibility Checklist

| Check | Evidence | Verdict |
|-------|----------|---------|
| Checklist covers Phase 1 spike minimum capabilities | `gc3_import_deferred.md` lines 29-37: 7 capabilities (create, update state, add comment, query comments, artifact path ref, label management, JSON output) | PASS |
| Pass/fail criteria defined | Lines 41-43: items 1-5 must pass; fallback if #3 unavailable; fail if #1 or #2 unavailable | PASS |

### 5. Tests

| Check | Evidence | Verdict |
|-------|----------|---------|
| `tests/growth/test_contracts.py` all pass | 54/54 passed (0.06s). Covers: PassRuleKind, construction, frozen, soul profile, skill_spec profile, wrapper_tool V1 (10 tests), skeleton historical (3 tests), reserved skeletons, get_contract registry, eval result pinning, immutability invariants. | PASS |
| `just lint` clean | ruff: "All checks passed!"; complexity: 0 block findings, 0 regressions | PASS |

## Residual Risks

1. **bd feasibility spike not yet executed**: The checklist exists but is unverified. Phase 1 depends on bd capabilities being confirmed. If the spike fails, the fallback path (artifact-first + bead-pointer-only per ADR 0055) is already documented.
2. **Version numbering ambiguity**: Both skeleton and V1 use `version=1` with different `contract_id`. This is correct by design (version is per-contract, not per-kind), but could confuse tooling that doesn't use `contract_id` as the primary discriminator.

## Gate Recommendation

**PASS**. All 5 core acceptance criteria are met with direct evidence. No P0/P1 findings. The Phase 0 deliverables correctly establish the GLOSSARY alignment, formal wrapper_tool eval contract, GC-3 deferral rationale, and bd feasibility checklist as documented foundations for Phase 1.
